name: main

on:
    push: 
        branches: ["main"]
    pull_request: 
        branches: ["main"]

jobs:
    docker:
        runs-on: ubuntu-latest

        steps:

            - uses: ./.github/actions/build-container/actions.yml
